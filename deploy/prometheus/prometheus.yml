global:
  scrape_interval: 2s
  evaluation_interval: 2s

scrape_configs:
  - job_name: 'loadflow-demo'
    static_configs:
      # Go demo 运行在宿主机上，通过 host.docker.internal 访问
      - targets: ['host.docker.internal:2112']
    scrape_interval: 2s
